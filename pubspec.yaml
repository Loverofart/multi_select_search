name: multi_select_search
description: A Flutter package that contains search text field and a list of items where you can select multiple items.
version: 0.0.1
homepage: https://github.com/Loverofart/multi_select_search

environment:
  sdk: ">=2.18.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
